version: 1
backend:
  phases:
    preBuild:
      commands:
        - npm i
    # IMPORTANT - Please verify your build commands
    build:
      commands: []
    postBuild:
      commands:
        - npm start
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
