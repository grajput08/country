version: 1
backend:
  phases:
    build:
      commands:
        - npm install # or yarn install, depending on your package manager
  artifacts:
    backendDirectory: .
  cache:
    paths:
      - node_modules/**/*
  build:
    commands:
      - npm start
